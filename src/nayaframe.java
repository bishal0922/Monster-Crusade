import javax.swing.*;

public class nayaframe extends JFrame {
    public nayaframe(){
        this.setDefaultCloseOperation(JFrame.EXIT_ON_CLOSE);
        this.setLayout(null);
        this.setSize(500,500);
        //set this at the end of the program to make it visible as it doesn't show at the start
        this.setTitle("new window idk");
        this.setVisible(true);
    }

}
